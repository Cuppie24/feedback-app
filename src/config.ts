// App-wide constants sourced from the Vite env. Values are baked in at
// build time. The fallback is user-facing copy, hence the Cyrillic.

export const APP_NAME = import.meta.env.VITE_APP_NAME || 'Micros Обратная связь'
